export interface Todo {
    id: number;         // Unique identifier for the todo
    title: string;      // Title or description of the todo
    completed: boolean; // Status to indicate if the todo is completed or not
  }